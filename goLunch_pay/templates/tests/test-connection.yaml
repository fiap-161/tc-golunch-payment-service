apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goLunch.fullname" . }}-test-connection"
  labels:
    {{- include "goLunch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goLunch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
